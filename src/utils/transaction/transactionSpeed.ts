/**
 * @deprecated This file is deprecated. Use utils/transaction/speedOptions.ts instead.
 * This file remains for backward compatibility but forwards to the real implementation.
 */

import { getSpeedOptions, speedOptions } from '@/src/utils/transaction/speedOptions'

// Re-export the real implementations
export { getSpeedOptions, speedOptions } 